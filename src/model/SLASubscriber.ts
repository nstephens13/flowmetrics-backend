/**
 * @prop {number} id the id of the SLA subscriber
 * @prop {string} name the name of the SLA subscriber
 * @prop {string} description the description of the SLA subscriber
 */
export interface SLASubscriber {
  id: number | null;
  name: string | null;
  description: string | null;
}
